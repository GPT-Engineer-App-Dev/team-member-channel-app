import { Box, Heading, Text } from "@chakra-ui/react";

const Channel = () => {
  return (
    <Box p={4}>
      <Heading mb={4}>Channel Settings</Heading>
      <Text>Welcome to the Channel page. Manage your communication channels here.</Text>
    </Box>
  );
};

export default Channel;